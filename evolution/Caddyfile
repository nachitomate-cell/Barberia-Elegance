# Reverse proxy con HTTPS automático (Let's Encrypt) para Evolution API.
# El dominio se inyecta por la variable de entorno EVOLUTION_DOMAIN.
{$EVOLUTION_DOMAIN} {
    encode gzip
    reverse_proxy evolution-api:8080
}
